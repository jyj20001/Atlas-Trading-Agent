# ── Python ──
__pycache__/
*.py[cod]
*.pyo
*.egg-info/
dist/
build/
*.egg
*.manifest
*.spec

# ── Virtual Environment ──
venv/
.venv/
env/

# ── IDE ──
.idea/
.vscode/
*.swp
*.swo

# ── OS ──
.DS_Store
Thumbs.db

# ── Environment ──
.env
.env.local
*.key
*.pem
*.crt
*.cert

# ── Secrets (env-var-based, but keep dir clean) ──
config/local.yaml

# ── Output (scan results, reports, signal history) ──
output/
output/*

# ── Logs ──
logs/
logs/*

# ── Cache ──
data/.cache/
news_cache.json
docket_cache.json
price_cache.json

# ── Local Database (SQLite) ──
*.db
*.db-shm
*.db-wal
*.sqlite
*.sqlite3

# ── Alibaba Risk Monitor: cached files ──
alibaba_risk_monitor/news_cache.json
alibaba_risk_monitor/docket_cache.json

# ── Alibaba Risk Monitor: output ──
alibaba_risk_monitor/output/*

# ── Alibaba Risk Monitor: logs ──
alibaba_risk_monitor/logs/*

# ── Test artifacts ──
.coverage
.coverage.*
htmlcov/
.pytest_cache/
*.coverage
coverage/

# ── Ad-hoc verification scripts ──
scripts/hermes-verify-*

