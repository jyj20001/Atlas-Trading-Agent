# AGENT.md — Atlas Trading Agent 人格文件

> 本文件是 AI Agent（Hermes / Claude / ChatGPT 等）在 Atlas Trading Agent 项目中应遵循的长期人格、工作原则和行为规范。
>
> **当新 Session 启动时，请先读取 MEMORY.md 恢复上下文。**
> **不要修改策略、评分、参数或交易逻辑。**

---

## 项目目标

Atlas Trading Agent 是一个 A 股 Buy Stop 趋势突破自动扫描系统。它的核心目标不是赚快钱，而是**每天稳定运行、持续输出高质量信号、严格风控、不断学习进化**。

## 核心原则

1. **工程可靠性优先** — 这是一个长期运行的生产系统。稳定性 > 功能数量。
2. **信号质量 > 信号数量** — 宁可每天 0 候选，也不错发 1 个假突破。
3. **数据源中立** — 任何外部 API 可能随时变更。使用 curl 子进程 + 标准 HTTP 减少依赖。
4. **可复现性** — 同一输入必须产生同一输出。避免随机性、非确定时间依赖。
5. **零外部依赖** — 核心代码只使用 Python 标准库。所有第三方依赖均为可选。

## 交易原则

| 原则 | 说明 |
|------|------|
| **Buy Stop 为主** | 只做突破买入，不做抄底、不做反弹、不做左侧。 |
| **趋势为王** | 价格必须在 MA200 以上，趋势是唯一的朋友。 |
| **阶段决定仓位** | EARLY_BREAKOUT = 最佳窗口，TRENDING = 谨慎参与，EXTENDED/CLIMAX = 放弃。 |
| **量价配合** | 突破必须放量（量比 >= 1.5），缩量突破是假突破。 |
| **换手率评估** | 不同市值区间有不同换手率范围阈值。过高换手 = 出货。 |
| **风控为先** | 连续涨停 >= 3 天 → 排除。5日涨幅 > 50% → 排除。5日涨幅 > 30% → 标记。 |
| **基本面加分** | 业绩预增 / 快报 / 重大合同 / 回购增持 → 加分。预减预亏 → 减分。 |
| **市场环境适配** | 熊市仅 105+ 分可参与，牛市 C 级评分也可考虑。 |
| **T+1 限制** | A股当日买入不可卖出，必须考虑隔夜风险。 |

## 130 分评分体系

| 维度 | 满分 | 说明 |
|------|:----:|------|
| Technical | 100 | 趋势 20 + 结构 25 + 量能 20 + 换手 15 + 风险 10 + 板块 10 |
| Fundamental | 15 | 业绩预增 / 快报 / 重大合同 / 回购增持（含时间衰减） |
| Market | 5 | 沪深300 / 上证 / 创业板三大指数趋势加权 |
| Sector | 10 | 个股 vs 板块 5 日超额收益 |
| **Combined** | **130** | |

### 评级标准

| 评分 | 评级 | 建议 |
|:----:|:----:|------|
| >= 105 | A+ | Buy Stop 候选，最佳信号 |
| >= 95 | A | Buy Stop 候选，强信号 |
| >= 85 | B+ | 谨慎参与，需额外确认 |
| >= 75 | B | 仅观察 |
| < 75 | C / NO | 不参与 |

## 禁止事项

> **以下操作绝对禁止。任何违反将导致不可预测的交易风险。**

1. **禁止修改任何交易策略参数** — 包括 MA 周期、VOLUME_RATIO、CONSOLIDATION_LOOKBACK、BREAKOUT_WINDOW_DAYS、MAX_CONSECUTIVE_LIMIT、MAX_5D_CHANGE_PCT、TURNOVER 阈值。如需修改，必须先提交 Issue 讨论，经验证后才能变更。
2. **禁止修改评分逻辑** — 包括各维度权重、评级阈值、硬性过滤规则。评分体系是整个系统的基石。
3. **禁止修改 Buy Stop 规则** — 包括 Buy Stop 价格计算方式、止盈止损计算方法、突破识别逻辑。
4. **禁止新增交易策略** — Atlas 只做 Buy Stop 突破策略。如需新增其他策略，必须创建独立项目。
5. **禁止修改风控规则** — 包括连续涨停过滤、5日涨幅过滤、致命风险标记等。
6. **禁止硬编码密钥** — API Key、Webhook URL 必须通过环境变量读取。
7. **禁止在生产环境自动修改代码** — 任何代码修改必须经过测试验证。
8. **禁止在没有测试的情况下 Merge** — 主分支必须保持可运行状态。

## 代码修改规范

| 类型 | 要求 |
|------|------|
| Bug 修复 | 修复前必须复现 bug，修复后必须新增测试 |
| 性能优化 | 必须有性能数据对比（before/after） |
| 文档补充 | 不限制，随时可以补充 |
| 依赖升级 | 必须测试回退兼容性 |
| 新增功能 | 必须先创建设计文档，经评估后实施 |
| 重构 | 必须保持行为不变，通过全部测试 |

### 代码风格

- 严格遵守 PEP 8（用 ruff 或 black 格式化）
- 所有公共函数必须有 docstring
- 日志使用结构化 logging（不用 print）
- 类型注解优先使用 typing 模块
- 异常必须被显式捕获，不使用裸 `except:`

## 开发原则

1. **测试先行** — 核心模块必须 80%+ 覆盖率。每次修改必须运行测试套件。
2. **增量修改** — 每次只改一个模块，保持 diff 最小。
3. **向后兼容** — 不破坏现有输出格式、配置文件格式、环境变量。
4. **变更记录** — 所有对用户可见的变更必须更新 CHANGELOG.md。
5. **数据驱动决策** — 任何参数变更必须有回测数据支持。
6. **简单胜于复杂** — 能用一个函数解决的问题不要创建一个类。
7. **不做无意义的抽象** — 不提前设计未来可能用到的接口。

## 与 Hermes 协作

- 新的 Hermes Session 启动时，必须读取 `AGENT.md` 和 `MEMORY.md`
- 扫描结果通过企业微信推送（cron 定时任务）
- 系统日志输出到 `logs/` 目录（按日轮换）
- 扫描结果输出到 `output/` 目录（JSON + Markdown + CSV）
- 工程体检报告应写入 `docs/` 目录

## Development Workflow

本项目的开发规范定义在 `docs/development_workflow.md`，包含：

- **AI Agent 工作流程** — 需求确认 → 读取规范 → 修改代码 → 测试 → 提交
- **Git 规范** — commit 格式、分支策略、tag 规则
- **测试要求** — 必须运行的测试套件和报告格式
- **数据安全规则** — 禁止提交数据库、日志、输出文件
- **交易策略保护规则** — 核心文件列表和修改限制
- **文档维护规则** — 版本更新时需同步的文件
- **发布流程** — 从开发到发布的完整流程

**重要：任何代码修改前，请先阅读 `docs/development_workflow.md`。**
